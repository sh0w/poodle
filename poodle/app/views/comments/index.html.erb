<h1>Listing comments</h1>

<table>
  <tr>
    <th>Rating</th>
    <th>Content</th>
    <th>User</th>
    <th>Page</th>
    <th>Course</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.rating %></td>
    <td><%= comment.content %></td>
    <td><%= comment.user_id %></td>
    <td><%= comment.page_id %></td>
    <td><%= comment.course_id %></td>
    <td><%= link_to 'Show', [@course,comment] %></td>
    <td><%= link_to 'Edit', edit_polymorphic_path([@course,comment]) %></td>
    <td><%= link_to 'Destroy', [@course, comment], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Comment', new_course_comment_path %>
