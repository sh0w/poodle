<%= form_for ([@course, @lesson, @page, @link]), remote: true do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= fields_for( @resource ) do |resource_f| %>
    <div class="field">
      <%= resource_f.label :headline %><br />
      <%= resource_f.text_field :headline %>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
