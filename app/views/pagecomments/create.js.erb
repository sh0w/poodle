$("#new_comment_link").show();
$('#new_pagecomment').remove();
var pagecomment = '<%= j render :partial => "pagecomment", :object => @pagecomment %>';

$(function(){
    var $boxes = $(".comment").last;
    $('#comments').append( pagecomment ).masonry( 'reload' );
});

