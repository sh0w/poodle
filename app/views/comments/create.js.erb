$("#new_comment_link").show();
$('#new_comment').remove();

var comment = '<%= j render :partial => "shared/comment", :object => @comment %>';

$(function () {
    var $boxes = $(".comment").last;
    $('#comments').append(comment).masonry('reload');
});