$('#page_<%= @page.id %>').remove();
//update_page_position()

//neue Breite für #pages bestimmen (für horizontales Scrollen)
var pageswidth = $('#pages').css("width").replace('px', "");
newpageswidth = (parseInt(pageswidth)-225) + "px";
$('#pages').css("width", newpageswidth);
